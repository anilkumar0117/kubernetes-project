apiVersion: v1
kind: Service
metadata:
  name: mysite
  labels:
    app: mysite

spec:
  type: ClusterIP

  selector:
    app: mysite

  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
